# Trained Models

This directory contains trained YOLOv8 model weights and artifacts.

**Note:** Model files (`.pt` files) are typically large and should be tracked with Git LFS or excluded from git.

